name: Android Kernel CI (Tab A9+)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 🧹 Maximize Build Space
        run: |
          sudo docker image prune -a -f
          sudo rm -rf /usr/share/dotnet /opt/ghc "/usr/local/share/boost"
          df -h

      - name: 🛠️ Set Up Build Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential zip curl libstdc++6 git wget 7zip python3 libssl-dev libelf-dev
          
          # Use the official Google stable Clang (Common for Android 11/12 kernels)
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86 clang-google
          
          # Keeping the Lineage GCC (This one usually stays public)
          git clone --depth=1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9 aarch64-linux-android-4.9
          
          # Update the PATH to match the new folder name
          echo "$GITHUB_WORKSPACE/clang-google/clang-r383902/bin" >> $GITHUB_PATH
          echo "$GITHUB_WORKSPACE/aarch64-linux-android-4.9/bin" >> $GITHUB_PATH
          echo "ARCH=arm64" >> $GITHUB_ENV
          echo "SUBARCH=arm64" >> $GITHUB_ENV

      - name: 📦 Reconstruct and Merge Source
        run: |
          echo "🔗 Combining Kernel pieces from root..."
          # Since the log shows files are in the main folder, we 'cat' them here
          cat Kernel.zip.0* > Combined_Kernel.zip
          
          echo "🔗 Combining Platform pieces from root..."
          cat Platform.zip.0* > Combined_Platform.zip
          
          mkdir -p kernel-source
          echo "📦 Extracting Kernel..."
          7z x Combined_Kernel.zip -okernel-source/
          
          echo "📦 Merging Platform into source..."
          7z x Combined_Platform.zip -aoa -okernel-source/
          
          # Clean up the big temporary zips
          rm Combined_Kernel.zip Combined_Platform.zip
          echo "✅ Source tree merged."


      - name: ⚙️ Compile Kernel
        run: |
          cd kernel-source
          CROSS_COMPILE_PATH="$GITHUB_WORKSPACE/aarch64-linux-android-4.9/bin/aarch64-linux-android-"
          
          DEFCONFIG_PATH=$(find . -name "gta9pwifi_eur_open_defconfig" -type f | head -n 1)
          cp "$DEFCONFIG_PATH" arch/arm64/configs/tmp_defconfig
          
          # Use 'yes' to handle any legacy config prompts automatically
          yes "" | make O=out ARCH=arm64 tmp_defconfig
          
          echo "🔥 Starting Build..."
          make -j$(nproc --all) O=out \
            ARCH=arm64 \
            CROSS_COMPILE="$CROSS_COMPILE_PATH" \
            CC=clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            HOSTCC=gcc \
            HOSTCXX=g++ > ../build.log 2>&1 || (tail -n 100 ../build.log && exit 1)


      - name: 📤 Upload Result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kernel-build-output
          path: |
            kernel-source/out/arch/arm64/boot/Image*
            build.log
